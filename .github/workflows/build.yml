# Builds the debug APK on GitHub's runners so you don't need Android Studio
# or a local SDK. Push this repo, then grab the APK from the run's Artifacts.
name: Build APK

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # AGP 9.x requires JDK 17+; 21 is the current LTS and what Gradle 9 prefers.
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Generate wrapper if missing
        run: gradle wrapper --gradle-version 9.7.1

      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ringcursor-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
